nexus:
  docker:
    enabled: false
    registries:
      - host: localhost
        port: 5000
ingress:
  enabled: true
  # Can't just use "", it'll just place it as raw string lolz
  #hostRepo: '"*."'
  annotations:
    kubernetes.io/ingress.class: nginx
  hostRepo: nexus.demo
  hostPath: /
  #tls:
  #  secretName: false
  